import { createTheme } from '@material-ui/core/styles'


const theme = createTheme({
  palette: {
    primary: {
      main: "#f44336",
      light: "#fafafa",
      dark: "#aa2e25",
    },
    secondary: {
      main: "#11cb5f",
      light: "#FFFFFF",
      dark: "#000000",
    },
  },
});

export default theme

